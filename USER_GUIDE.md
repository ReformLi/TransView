# 传视(TransView) 使用指南

## 一、安装与首次启动

1. 将 `app-debug.apk` 安装到 Android TV / 电视盒子（U盘或 adb install）。
2. 首次启动会请求存储权限：
   - 点击「去授权」→ 系统设置页 → 开启「允许管理所有文件」→ 返回 App。
3. 授权后进入主界面，右上角显示「服务器运行中」，通知栏出现常驻通知「文件服务器运行中」。

## 二、手机上传（无需安装任何 App）

1. 手机连接与电视**相同的 Wi-Fi**。
2. 电视端「上传」页**顶部固定区**有二维码、**访问码**与服务器地址，两种进入方式任选：
   - **扫码（推荐，零输入）**：直接扫二维码，打开即是上传界面，不用输任何东西。
   - **手动输入地址**：照着屏幕在手机浏览器输入地址（如 `http://192.168.1.100:2333`），也可以按地址下方的「复制链接」按钮把链接复制走。
     这种方式打开后**需要输入电视屏幕上显示的访问码**（二维码下方那条**蓝色横条**里的 6 位，如 `A3F9K2`，字母+数字，不分大小写），点「确认」通过后才能上传。输错了会提示「访问码错误，请重试」。
3. **关于访问码**：访问码在电视端 App **打开期间固定不变**——熄屏、播放视频、休眠唤醒、改端口都不换码，只有**重启电视端 App** 才会换新码。手机网页同标签页刷新不用重输。若网页提示「认证已失效，请重新输入电视屏幕上显示的访问码」，说明电视端 App 重启过，照屏幕重输或重新扫码即可，**已排队的文件会自动接着传**。
4. 在网页上选择分类：
   - **视频**：仅接受 mp4、mkv、avi、mov、flv、wmv 等 **+ zip 压缩包**
   - **图片**：仅接受 jpg、png、gif、webp 等 **+ zip 压缩包**
   - **其他**：接受除视频图片外的所有文件（apk、pdf、zip、音乐等）；这里的 zip **原样保存、不解压**
5. 三种添加方式：
   - 「选择文件」：多选文件
   - 「选择文件夹」：整个文件夹（保留层级，如整部电视剧）
   - 拖拽：把文件或文件夹拖到虚线框（电脑浏览器同样适用）
   不符合当前分类的文件会自动忽略并提示。
6. **上传 zip 压缩包（视频 / 图片分类）**：电视端会自动解压，**只保留当前分类的文件**，并按压缩包里的文件夹结构放好，其余文件丢弃。上传完成后网页与电视都会提示结果：
   - 成功：「已从压缩包解压 N 个视频文件，原压缩包已删除」；
   - 没找到对应文件：「压缩包中未找到视频文件，已保留原压缩包」——原包会放到「其他」分类；
   - 空间不足：「存储空间不足，已保留原压缩包」，原包同样保留在「其他」分类。
   解压失败的任何情况都**不会丢你的压缩包**。想把 zip 原样保存（不解压），就选「其他」分类上传。
7. 每个文件独立显示进度条与状态（等待/上传中/成功/失败/已取消），逐个自动上传。
8. **每个文件都能单独「取消」和「重试」**（每行右侧按钮）：
   - **取消**：排队中的文件直接从列表移除；**正在上传的会立刻断开**（不是只改显示），该文件变为灰色「已取消」。
   - **重试**：失败或已取消的文件点「重试」即可重新上传，**沿用原文件与文件夹层级，不用重新选**；重试的项会插到队列最前面，马上开始。
9. **切换到视频/图片/其他标签不会中断上传**；回到「上传」页，记录列表实时显示每个文件的进度条与百分比。
10. 网页头部与**浏览器标签页图标**是同一枚矢量标识（蓝底播放三角），方便在多个标签页里认出来。

## 三、上传记录管理

「上传」页顶部固定区下方是**上传记录列表**，每条记录显示：文件名、大小、进度（上传中显示进度条+百分比）、状态（等待中/上传中/成功/失败）、上传时间、分类。

> 手机端点了「取消」的那次上传，电视端会记为**失败**——服务端只能看到连接被中断，无法区分「用户主动取消」和「网络断开」，两者都按未完成处理；手机端点「重试」成功后会另生成一条成功记录。

- **删除单条记录**：焦点移到某条记录上，按遥控器**菜单键或删除键**（或选中记录右侧的「删除」按钮）→ 弹窗确认。
  **删除记录只清除历史日志，电视上的文件原样保留**，确认后提示「已删除记录，本地文件已保留」。
  删除后焦点会自动落到**下一条**记录（删的是最后一条则落到上一条），可以直接接着删，不用重新把焦点移回来。
- **清空所有记录**：列表右上角「清空所有记录」按钮，弹窗确认后一次性清空（同样不影响本地文件）。
- 记录按时间倒序，最多保留最近 200 条。
- **遥控器上下键**：焦点在记录列表**第一行**时按 ↑，先到列表右上角的「清空所有记录」，再按 ↑ 回到顶部「上传」标签；从「上传」标签按 ↓ 可原路返回。
- **用触摸屏或鼠标点过屏幕后，遥控器照样能用**：点按内容区后按方向键，焦点会落在本页第一个元素上继续操作，不会跳到顶部标签、也不会把页面切走。

## 四、媒体库浏览

- 顶部四个标签：**上传 / 视频 / 图片 / 其他**，遥控器左右键切换。
- 视频 / 图片 / 其他页均为**多列网格卡片**（电视宽屏一屏展示尽量多的内容）：文件夹卡片显示文件夹图标与文件数，视频卡片显示首帧缩略图与「时长·大小」，图片卡片显示缩略图，其他文件显示类型图标。
- 已看过的视频，卡片底部会有一条细进度条，直观显示观看进度。
- 页面顶部工具条：左侧是**路径面包屑**（如「视频 > 甄嬛传」），右侧是「排序：xxx」与「刷新」两个按钮。
- 卡片聚焦时会放大并出现高亮边框；按**确定键**直接打开（文件夹进入 / 视频播放 / 图片查看 / 其他文件用系统应用打开）。
- **文件名太长怎么办**：未选中时名称按「…」截断；用遥控器**把焦点移到这张卡片上，名称会来回循环滚动**，可以完整看清。移开焦点即停止滚动，短文件名不会滚动。
- 进入文件夹后，焦点会自动落在第一个文件/文件夹上，按**返回键**或选中网格里的「返回上级」卡片即可回到上一级；返回后焦点会自动落回你刚才进入的那个文件夹，不会丢失。
- **遥控器上下键**：焦点在网格**第一行**时按 ↑，先到本页工具条（排序 / 刷新），再按 ↑ 回到顶部**本页对应的标签**（在视频页就是「视频」，不会再跳回第一个「上传」）；按 ↓ 可逐级返回网格。中间几行按 ↑ 只是在网格内往上走。
- **遥控器左右键**：只在卡片之间左右移动；到了每行最左或最右的卡片会停在原地，不会跳回顶部菜单或误切页面。
- 所有文件存放在电视的专属目录 `/sdcard/TransView/` 下：视频在 `TransView/Movies`，图片在 `TransView/Pictures`，其他在 `TransView/Downloads`（不占用、不扫描系统目录）。
- 想把 U 盘/电脑里的电影搬进电视：直接拷到 `/sdcard/TransView/Movies/`（或其子文件夹），在视频页点「刷新」即可显示（每次启动也会自动核对）。
- **分类目录只放对应格式**：`Movies` 只显示视频、`Pictures` 只显示图片、`Downloads` 只显示「其他」（非视频非图片）。若把视频放进 `Pictures`、或把 txt 放进 `Movies`，对账时会**自动跳过**（既不显示、也**不会删除文件**）；想让它出现，把它移到对应的分类目录再点「刷新」即可。
- 同名文件夹上传时自动合并，同名文件自动加 `(1)(2)` 后缀，**绝不覆盖**已有文件。
- **文件操作菜单**：焦点移到某个文件/文件夹卡片上，按**菜单键**或**长按确定键**即可弹出操作菜单（打开/播放/查看、删除、取消）；**短按确定键**仍是直接打开（进入文件夹 / 播放视频 / 查看图片 / 系统打开）。
- **长按的判定**：按住约半秒（400ms）后**松开**弹出菜单；不足半秒算短按。键盘（模拟器）同样支持：按住 **Enter / 空格 / 小键盘 Enter** 半秒以上松开即可，短按打开。
- **删除文件**：焦点移到某个文件卡片上，按**菜单键**（或**长按确定键**）弹出选项→删除，或遥控器**删除键** → 弹窗确认 → 删除电视上的物理文件（不可恢复）；若删除后所在文件夹变空，空文件夹一并清理。
- **排序**：点工具条「排序」按钮或按菜单键（焦点不在卡片上时），可在名称（A-Z / Z-A）、时间（新→旧 / 旧→新）间切换。
- **刷新媒体库**：点工具条「刷新」按钮，重新核对文件与索引的一致性（清掉已删除文件的残留记录、补入新放入的文件、自动清理空文件夹，完成后提示统计结果）。对账进行中按钮会变成「对账中…」并置灰，此时再按不会重复触发；**焦点会留在按钮上，页面不会跳走**。
- App 每次启动也会自动做一次同样的核对；上传完成、删除文件后列表实时刷新，无需手动操作。

## 五、视频播放

- 打开视频即播放。控制栏按 6 秒无操作自动隐藏，动一下遥控器或按菜单键即可调出。
- **控制栏是「时间轴优先」三段结构**（对齐 Netflix / tvOS 的主流习惯），自上而下：
  1. **内联选择器**：按「倍速 / 比例」时在时间轴上方展开的横排胶囊选项（用完即收，不是弹框）；
  2. **时间轴**：可聚焦的进度条——**控制栏弹出时焦点默认落在时间轴上**，左右键直接拖动进度；
  3. **按钮行**：上一集 / 快退 / 播放暂停 / 快进 / 下一集，以及 倍速 / 比例 / 设置（⚙，音轨 / 字幕都收在里面）。
  播放暂停图标按「**按下去会发生什么**」显示：正在播放时显示暂停图标（按下会暂停），暂停时显示播放图标（按下会播放）。
- 遥控器操作：
  - **控制栏隐藏时**：左右键 = 快退 / 快进；中键 = 播放 / 暂停；上 / 下 / 菜单键 = 唤出控制栏（焦点落时间轴）。
  - **控制栏显示时**：
    - 焦点在**时间轴**上：左右键 = 拖动进度（带缩略图预览，见下）；中键 = 播放 / 暂停；下键 = 进入按钮行。
    - 焦点在**按钮行**上：左右键在按钮之间移动焦点，中键确认；上键 = 回时间轴。
    - **边缘收起**：时间轴上再按一次上键、或按钮行上再按一次下键 = 收起控制栏（下一集倒计时预告卡在场时不收，防误触）。
    - 移动焦点会重新计时，不会操作到一半控制栏就消失。
  - **菜单键**：控制栏隐藏时唤出；显示时收起；下一集预告卡在场时先关掉预告卡。
  - **遥控器 ⏪ / ⏩ 键**：快退 / 快进（不受控制栏状态影响）。
  - **遥控器 ⏯ / ⏭ / ⏮ 键**：播放暂停 / 下一集 / 上一集（「上一集 / 下一集」在当前是第一集 / 最后一集时会变灰，焦点仍能停在上面，按确定键没有反应，属正常）。
  - **返回键分层**：有预告卡先关预告卡 → 有设置面板先关面板 → 有内联选择器先收选择器 → 控制栏显示则收起 → 再按一次退出播放。
- **快进 / 快退带画面预览**：屏幕中央出现预览卡——**将要跳到的那一帧缩略图** + 方向图标 +「目标位置 / 总时长」（例如 `01:21 / 13:01`），拖动不再是盲跳；松手后预览卡停留约 1 秒淡出。短按一次固定跳 10 秒（连按多少下都是一下 10 秒）；按住不放进入变速扫描，会越来越快。连续快进不会弹出控制栏，操作不被打断。
- **倍速 / 画面比例**：按钮行的「倍速」「比例」是文字按钮（改成非默认值后按钮直接显示当前值，如 `1.5x`、`裁剪`），按确定在时间轴上方展开横排胶囊，焦点落在当前选中项；再按确定**即选即生效并收起**（上键 / 返回键直接收起，焦点回按钮）。倍速 0.5x ~ 2.0x；比例为**原始（保持比例、留黑边）/ 拉伸（铺满、画面变形）/ 裁剪（铺满、裁掉边缘）**。这里改的是本次播放，默认值在**设置 → 播放设置 → 默认倍速 / 默认画面比例**里改。
- **音轨 / 字幕**：按钮行的 **⚙ 设置**按钮唤出右侧滑入面板，内含「音轨 / 字幕」两个分区（字幕区恒有「关闭字幕」），上下键选择、确定即生效并收起、焦点回到 ⚙；视频没有多音轨 / 字幕时面板显示占位提示。
- 暂停后屏幕中央会一直显示播放图标（上下左右居中），方便确认当前处于暂停状态；此时按**中键 / 确认键**即恢复播放，图标随之消失。
- 看到一半退出会自动记录进度，再次打开提示「是否从上次位置继续播放」；看到片尾（最后 5 秒内退出）视为已看完，不再提示。（提示可以在**设置 → 播放设置 → 自动续播提示**里关掉，关掉后打开视频会自动续播、不弹窗。）
- **播完一集：下一集预告卡**（自动连播开启时）：右下角弹出预告卡——下一集**缩略图 + 标题 +「N 秒后自动播放」**的 5 秒倒计时，焦点落「立即播放」；等倒计时归零自动播下一集，不想等直接按确定。按「取消」或返回键作废倒计时，停在片尾（屏幕提示「已取消自动连播」），此时可点「播放/重播」再看一遍，或按返回键离开。播放列表为同文件夹按文件名自然排序（EP2 在 EP10 之前）。最后一集播完会停在片尾并弹出控制栏。不想自动连播可在**设置 → 播放设置**里关闭。
- 字幕：把与视频同名的 `.srt/.ass/.ssa/.vtt` 放在同一文件夹即可自动加载。

## 六、图片查看

- 打开图片全屏显示；**左右键**切换上/下一张；**中键**在 1x/2x 间切换缩放；**菜单键**循环缩放（1→1.5→2→3 倍）；放大后方向键平移。
- 从查看器/播放器**返回列表时，焦点会定位到最后浏览（播放）的那一项**——比如打开第 3 张后切到第 7 张，返回后列表停在第 7 张上；视频自动连播到后面几集再退出，同样定位到最后播放的一集。
- **返回键分层**：轮播可见时先收起轮播 → 图片处于放大状态时先**复原为 1x** → 都不是才退出回图片列表。
- **上键**：全屏整洁模式（隐藏顶部文件名栏），再按上键恢复顶栏。
- **下键**：唤出底部**同目录缩略图轮播**并直接进入选择模式——**高亮取景框固定在屏幕正中不动**（当前图白框标记），左右键让缩略图从框下滑过并逐张居中（滑到开头/结尾时留白、到头即停），**确定键**切换到框内图片（轮播不隐藏，可继续选择）；按**上键或返回键**收起轮播，返回键在轮播收起后再按一次才退出查看器。
- 缩略图轮播只在**同一文件夹**内的图片间切换。

## 七、其他文件

- 「其他」分类里的文件按系统应用打开：APK → 安装器，PDF/文本 → 阅读器，音乐 → 播放器。
- 若系统没有对应应用，会提示「无法打开此文件类型，请安装对应应用」。
- **「其他」页里还会看到两个 App 自己产生的目录**：`app_log`（开启「App 调试日志」后的运行日志，按日期分目录，见「九、设置」）和 `crash_log.txt`（App 万一异常退出时自动写下的堆栈）。两者都只是普通文件，可以像别的文件一样打开或删除，**删掉不影响 App 运行**。

## 八、常见问题

| 现象 | 处理 |
|:--|:--|
| 扫码打不开页面 | 确认手机与电视同一 Wi-Fi；检查电视端「服务器运行中」状态；尝试手动输入地址（此时需输入屏幕上的访问码） |
| 手输网址后要求输访问码 | 正常：访问码显示在电视「上传」页二维码下方的**蓝色横条**里，共 6 位（字母+数字，不分大小写）。扫码进入则不需要输入 |
| 提示「认证已失效」 | 电视端 App 重启后访问码会换新码（平时熄屏/播放/休眠恢复不换）。照电视屏幕重新输入访问码或重新扫码即可，**已排队的文件会自动接着传**，不用重新选 |
| 访问码什么时候会变 | 电视端 App 重启才换新码（App 打开期间固定），避免旧链接长期有效。没有「记住访问码」功能，但同一标签页刷新不用重输 |
| 上传很慢 | 受 Wi-Fi 带宽限制，建议 5GHz 频段 |
| 上传失败 | 确认电视存储空间充足；若提示连接失败，可能是服务器已休眠或正在播放视频，在电视「上传」页唤醒/等待恢复后重试 |
| 遥控器没有菜单键 / 找不到「删除」入口 | 选中文件卡片后按「删除键」可直接删除；部分遥控器把「菜单」做在选项键上。也可**长按确定键**（约半秒）弹出操作菜单。Android Studio 电视模拟器可用键盘 **Ctrl + M** 代替菜单键、**Delete** 代替删除键 |
| 键盘长按 Enter 像多次确定 | 不会：按住 Enter/空格/小键盘 Enter **半秒以上再松开**即弹操作菜单（不足半秒算短按=打开）；若仍异常请确认安装的是最新构建 |
| 看不到刚传的文件 | 上传完成后列表会自动刷新；也可点媒体库右上角「刷新」按钮 |
| 换了 Wi-Fi 后地址变了 | 回到「上传」页自动刷新二维码与地址 |
| 电视待机后手机无法上传 | 默认智能模式待机时暂停接收：在 设置 → 服务器与网络 → 保活策略 改为「极速模式」即可待机仍可上传 |
| 升级后看不到旧版本传的文件 | 旧版存放在系统公共目录（Movies 等），新版只认专属目录 `/sdcard/TransView/`；用文件管理器把旧文件移到 `/sdcard/TransView/Movies/`（或对应分类目录），点「刷新」即可 |
| 系统目录里的文件出现在媒体库 | 不会出现：App 只扫描 `/sdcard/TransView/`，绝不扫描系统公共目录 |
| U 盘插上了，但设置里「存储位置」没有它 | v1.14 起 App **自动扫描**所有可写存储卷，插上一般就能在「存储位置」里看到（显示为 `盘名 (卷ID)`，副标题带「可用 / 共」容量），**不需要任何授权操作**。若确实没出现，说明系统没把这块盘的可写路径交给 App（不是"没插上"）：到 **设置 → 存储与数据 → 打开「App 调试日志」**，然后重插一次 U 盘并回到设置页（或拔插后在「其他」页点「刷新」），再到媒体库「其他」页打开 `app_log` 文件夹里的当天日志 —— 里面逐条记录了 App 找到的**候选卷根**、每个候选的 `canRead`/`canWrite`与**写探针结果**（写探针失败 = 不可写；候选里压根没有该盘 = 系统未上报）。日志可在 TV 上直接打开，也能拷到电脑上看 |
| 升级后设置里找不到「添加 U 盘（需授权）」和「手动指定 U 盘路径」了 | v1.14 起这两项已**删除**。原因：真机实测部分电视（如 Vidda）把系统的文件授权选择器整个屏蔽，「授权」这条路过不去；而 U 盘的物理路径其实是可读写的。现在改为 App **自动扫描**存储卷（见上一行），插上 U 盘即会出现在「存储位置」里，无需任何授权或手填。旧版本留下的授权记录 / 手动路径设置会自动清理，不影响已有文件与媒体库记录 |
| U 盘拔了又插，需要重新设置吗 | 不用。App 在开机、拔插与每次对账前都会重新扫描，**U 盘插回后自动恢复**为存储位置；拔盘期间自动切回内部存储并提示（「降级模式」），上传不中断，媒体库记录一条都不会丢（只是暂时只显示内部存储上的文件，切回 U 盘就能看到）。 |
| 想反馈问题 / 想让日志留证据 | 设置 → 存储与数据 → 打开 **「App 调试日志」**（默认关）。开着的时候 App 会把关键运行日志写进 `TransView/Downloads/app_log/<日期>/<时间>.log`，之后到媒体库「其他」页进 `app_log` → 当天日期目录即可打开查看，也能拷到电脑。排查完建议**关掉**这个开关 |
| 调试日志会占满电视空间吗 | 不会。按天分目录、**单个文件最大 2MB**（写满自动续新文件）、**只保留最近 7 天**，更早的日期目录在启动日志功能时自动清理；清理范围只限 `app_log` 目录，**不影响「其他」页里的其他文件** |
| 打开日志后感觉卡顿 | 正常情况下不会：日志是**异步缓冲**写的（攒够 4KB 或空闲 1 秒才落盘一次），不阻塞上传 / 播放 / 对账；队列满了宁可丢日志也不会拖慢界面。若真的体感变慢，关掉开关即可立即停止（并清掉尚未落盘的内存队列） |
| 手动拷进分类目录的文件看不到 | 先确认放对了目录：`Movies` 只显示视频、`Pictures` 只显示图片、`Downloads` 只显示其他文件；格式与目录不符的文件会被跳过（不显示、也**不会删除**）。移到正确目录后回到媒体库点「刷新」即可 |
| 刷新时工具条上出现「正在扫描：xxx（N 项）」 | 正常：对账是**边扫边入库**，浅层文件会先出现。首次扫描需读取视频时长，文件很多时会稍慢；扫完会提示「扫描完成」 |
| 手机横屏时上传页二维码很小 / 地址显示成「…」 | v1.18 起上传页会**按屏高自动进入紧凑模式**（内容区高度 < 480dp 时）：收紧四周留白、把左栏加宽到 46%、省掉多余标题、**地址与「复制」并排一行**，二维码回到可扫码的尺寸（约 145dp）、地址整行显示（窄机型最多折两行）。若仍觉得二维码偏小，把手机转回**竖屏**，或直接用电视 / 平板查看即可 |
| 手机横屏下字体显得太大、一屏看不了几条 | v1.19 起，屏高不足 480dp 时 App 会把**内容区整体缩小**（约 87%）：视频 / 图片 / 其他 / 设置各页的字号与间距同步变小、比例不变，一屏能看到更多内容；上传页的记录行也变矮。顶部标签栏尺寸保持不变。电视 / 平板上不生效，显示与以前完全一样 |
| 手机横屏下设置页滑动不了：左边看不到「关于」、右边只能看到「存储空间占用」 | v1.20 起设置页的**左侧分组列表与右侧设置项列表各自可滚动**：用遥控器把焦点移到被挡住的项上，页面会自动滚动带进视野；手机 / 平板可以直接用手指拖动滑。一屏放得下时布局与以前完全一样（电视 / 平板不受影响） |
| 焦点突然跳到「服务器与网络」 | v1.21 修好了两个入口：① **点了一下空白处 / 用手指划动**时，触点没落在设置行上（行间空隙、左右留白、小字提示、卡片边缘），焦点现在会**留在原来那一项**上，不再跳回第一个分组；② 分组**最后一行按「下」**不再跳走，会停在原地 |
| 电视端上传进度一直显示 0%、成功了才变成 100% | v1.23 / v1.24 修好了：之前进度计数挂在了错误的位置（一是 NanoHTTPD 把文件直接按文件名写盘、绕过了计数器，二是计数器绑错了线程、后台根本读不到它），现已改为从**网络接收层**实时计数，上传过程中进度条会随已接收字节逐步增长 |
| 手机上屏幕最顶部（时间 / 电量那一条）看着发黑、时间看不清 | v1.25 修好了：App 是恒定深色界面，之前系统栏图标样式跟着手机系统的深浅模式走，系统浅色模式下图标被染成深色、压在深色背景上就看不清了。现在**状态栏 / 底部导航栏的图标固定为浅色**（白色），且不叠加系统灰底；电视上没有状态栏，不受影响 |

## 九、设置

顶部导航栏右侧的**「设置」**是独立设置页（不再是小弹框）。**焦点移到「设置」上页面即切换为设置页**（焦点仍在「设置」上），按 **↓** 进入设置内容；**返回键**随时退回之前的媒体页。

**页面布局**：左侧是分组列表（上下键切换分组），右侧是对应分组的设置项：

```
┌──────────────┬───────────────────────────────┐
│              │  ▸ 保活策略        智能模式     │
│  服务器与网络  │  ▸ 服务器端口      2333        │
│  播放设置     │  ▸ 开机自启        关闭         │
│  界面设置     │  ▸ 设备名称        客厅电视     │
│  存储与数据   │                               │
│  关于        │                               │
└──────────────┴───────────────────────────────┘
```

**操作方式**：分组上按 **→** 进入右侧设置项列表；设置项上按 **←** 回到左侧分组；按**确定键**（或 →）弹出选项，选完自动关闭、焦点回到刚才那一项。

**内容超出屏幕时可以滚动（v1.20）**：左侧分组列表与右侧设置项列表**各自可滚动**。用遥控器把焦点移到被挡住的那一项，页面会自动滚动把它带进视野；手机 / 平板上也可以直接用手指**拖动**滑动。**电视 / 平板上一屏放得下的内容显示与以前完全一样**（不会因为能滚动而改变位置）。

### 各分组内容

| 分组 | 设置项 | 状态 |
|:--|:--|:--|
| 服务器与网络 | 保活策略 / 服务器端口 / 开机自启 / 设备名称 | 全部即时生效 |
| 播放设置 | 自动续播提示 / 自动连播 / 默认倍速 / 默认画面比例 | 全部即时生效（下次打开视频可见） |
| 界面设置 | 网格列数 / 默认排序方式 | 全部即时生效（返回媒体库可见） |
| 存储与数据 | 存储位置 / 存储空间占用 / App 调试日志 / 清空上传记录 / 清空播放历史 / 手动触发对账 | 全部即时生效 |
| 关于 | 应用名与版本 / 声明 / 开源许可 / 致谢名单 | 直接内联显示，可聚焦、可滚动 |
| （两栏通用） | 分组 / 设置项多于屏幕时 | 各自可滚动：焦点移动自动带进视野，触摸可直接拖动 |

> **上传访问码没有开关**：它是防止同网段陌生设备往电视塞垃圾文件的底线门槛，恒定开启、不可关闭（见「二、手机上传」第 2–3 条）。

- **存储位置**：选择文件存到哪块盘。列表里**每一项都是 App 自动扫描出的、真正可写的设备**：「内部存储」恒在第一位，后面是外接盘（U盘 / SD卡），显示为 `盘名 (卷ID)`（如 `SDCARD (0000-0000)`），副标题写明「可用 X / 共 Y」。选中即生效（上传落盘、媒体库、播放全部跟着切）。选中的盘被拔出时**自动切回内部存储**并提示，上传不会中断；插回后自动恢复。期间媒体库只显示当前这块盘上的文件，**另一块盘上的记录都还在**，切回去就能看到。当前不可用的盘会显示成 `盘名（已断开）` 且**选不中**——这就是「降级模式」在设置页里的样子。
- **App 调试日志**（**默认关闭**）：开关，打开后 App 会把运行中的关键日志**异步**写进「其他」页对应的 `app_log` 目录（`/TransView/Downloads/app_log/<日期>/<时间>.log`），并取代了此前「导出存储诊断日志」那一项（那项已**彻底移除**）。开关下方有一行小字标明**当前**日志位置（内部存储，或 U 盘时的 `U 盘（盘名）/…`）与查看路径。**仅供排查问题，长期开着会略微增加耗电与存储写入**，平时建议关闭。日志按天分目录、单个文件最大 2MB（写满自动续新文件）、**只保留最近 7 天**（更早的自动清理）。查看方式：媒体库「其他」页 → `app_log` 文件夹 → 当天的日期目录 → 选中 `.log` 文件按确定，用系统应用打开；也可以在电脑上从存储里直接拷走。
  > 遇到「U 盘插上了但存储位置里没有它」这类存储问题，请**先打开这个开关**，再插拔一次 U 盘并回到设置页看一眼，然后把 `app_log` 里的日志拷给开发者。
- **崩溃日志**：App 万一异常退出，会把出错堆栈自动写进 `/TransView/Downloads/crash_log.txt`（同一个「其他」目录，重启后对账一次就能在 TV 的「其他」页看到，也能拷到电脑）。遇到「点某个按钮就闪退」时，把这个文件发给开发者即可定位，无需接 adb 看日志。
- **存储空间占用**：显示当前存储沙盒的已用大小，点击刷新。手动对账时会顺带清理解压临时目录（`.temp_unzip`）里的残留（解压途中断电会留下半个工作目录）。
- **清空上传记录 / 清空播放历史**：均需二次确认；清上传记录只删日志**不动物理文件**。
- **手动触发对账**：等同于媒体库「刷新」，核对文件与索引一致性。
- **关于**：内容**直接显示在页面里**，无需点开弹框——应用名与版本（v1.0.0）、声明、开源许可（MIT 全文）、致谢名单。每条信息都可用遥控器聚焦（按确定没有动作，只是展示）；条目较多时按上下键，右侧整栏会随焦点自动滚动（v1.20 起滚动的是**整栏**，滚到底能看到卡片完整的圆角底边）。左键回左侧分组，首条按上回到顶部「设置」标签。

- **服务器端口**：预设 **2333（默认）/ 5210 / 8080 / 8888 / 9527** 中单选，**切换后立即生效**——旧端口停止、新端口开始监听，「上传」页的二维码与地址会同步更新（手机端请用新地址重新扫码/输入）。若新端口被别的程序占用，会提示失败并**自动退回原端口**，不会把服务器改没了。旧版本安装升级后，若原先用的端口已不在预设列表（如 8081/9000），会自动回落到默认端口 2333。
- **开机自启**：开启后，电视每次开机自动拉起文件服务器，不用先打开 App。（应用首次安装后需要手动打开过一次。）
- **设备名称**：手机上传页顶部显示的名字，改完后手机端**刷新页面即生效**，无需重启服务器。
- **自动续播提示**：开启（默认）时，打开有历史进度的视频会先问「是否从上次位置继续播放」；关闭后不再询问，**直接从上次位置继续播**（进度照旧记录，只是不弹窗）。
- **自动连播**：开启（默认）时，一集播完先弹**下一集预告卡**（缩略图 + 5 秒倒计时，见「五、视频播放」），倒计时归零自动播同目录的下一个视频；关闭后播完**停在片尾**并弹出控制栏（屏幕提示「播放结束（自动连播已关闭）」），此时可按播放键重播、按「下一集」继续，或按返回键离开。
- **默认倍速**：设置**每次打开视频时的初始倍速**（1.0x / 1.25x / 1.5x）。播放过程中仍可用控制栏「倍速」按钮随时改成 0.5x~2.0x，临时改动不会写回设置。
- **默认画面比例**：设置**每次打开视频时的初始比例**——原始（保持比例、留黑边）/ 拉伸（铺满、画面变形）/ 裁剪（铺满、裁掉边缘）。播放过程中可用控制栏「比例」按钮临时切换，同样不回写设置。
- 以上四项都**下次打开视频时生效**（播放器在打开时读取设置），无需重启 App。
- **网格列数**：视频 / 图片 / 其他三个媒体页的卡片列数（4 / 5 / 6 列，默认 5 列）。屏幕大、想一屏看更多就调多；卡片太大就调少。**返回媒体库即生效**（列数同时决定卡片左右边界的手感，改完直接用遥控器试）。
- **默认排序方式**：媒体库打开时的排序（名称 A-Z / Z-A、时间新→旧 / 旧→新，默认名称 A-Z），三个媒体页各自独立。**这只是初始值**——在媒体页用工具条「排序」按钮做的调整只对本次浏览有效，切走再回来即回到这里设置的默认排序，不会被临时改动覆盖。

### 服务器保活策略（设置 → 服务器与网络 → 保活策略，修改立即生效并记忆）

| 模式 | 行为 |
|:--|:--|
| 极速模式 | 服务器始终运行，电视待机、播放视频时也能接收上传（最便利，略增后台占用） |
| 智能模式（默认） | 屏幕休眠或播放视频时自动暂停（结束后自动恢复）；15 分钟无上传自动休眠 |
| 省电模式 | 仅在「上传」页手动点击「启动服务器」才运行，离开上传页自动停止 |

- **正在上传的文件不会被掐断**：无论哪种模式，遇到屏幕休眠、播放视频、离开上传页，服务器都会**等文件传完才停**，不会中断传输。
- 智能模式休眠后，「上传」页顶部固定区变为「唤醒服务器」按钮，点击后 1 秒内恢复监听。
- 因播放视频暂停时无需任何操作，暂停播放或退出播放器后自动恢复。
- 通知栏文案实时反映服务器状态：运行中 / 已暂停 / 已休眠 / 已停止。
- 右上角指示灯同时显示运行状态与当前模式。
